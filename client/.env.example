# Local dev: leave empty — Vite proxies `/api` to the server (see vite.config.ts).
# Separate Vercel deployment: set to your API origin, no trailing slash, e.g.:
# VITE_API_URL=https://your-api-project.vercel.app

VITE_API_URL=
