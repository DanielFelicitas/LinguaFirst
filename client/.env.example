# Local:  cp client/.env.example client/.env — leave VITE_API_URL empty (Vite proxies /api).
# Vercel: set to your API origin, no trailing slash, e.g. same shape as Inventory Management System:
#   VITE_API_URL="https://your-api.vercel.app"
# Then redeploy the frontend.

VITE_API_URL=
